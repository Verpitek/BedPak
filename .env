# BedPak Environment Variables
# Copy this file to .env and fill in the values

# Required: Secret key for signing JWT tokens. Must be a secure random string.
JWT_SECRET=your-jwt-secret-key-here

# Required: Cloudflare Turnstile secret key for CAPTCHA verification.
# Get from Cloudflare Turnstile dashboard.
TURNSTILE_SECRET_KEY=your-turnstile-secret-key-here

# Optional: CORS allowed origins. Comma-separated list of domains.
# Example: "http://localhost:3000,https://example.com"
# Default: allow all origins (true)
# CORS_ORIGINS=

# Optional: Database file path. Default: bedpak.db in project root.
# DATABASE_PATH=bedpak.db

# Optional: Port to run server on. Default: 3000
# PORT=3000

# Development mode: Run server with `--dev` flag to skip CAPTCHA verification.
# This flag is passed as command line argument, not environment variable.
# Example: bun run --watch src/index.ts --devJWT_SECRET=test-secret-key-for-testing-purposes-only
